<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RestaurantePro.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RestaurantePro.Mobile.Features.Authentication.Pages"
    xmlns:ui="clr-namespace:RestaurantePro.Mobile.UI.Pages"
    xmlns:mesas="clr-namespace:RestaurantePro.Mobile.Features.Operations.Mesas.Pages"
    xmlns:comandas="clr-namespace:RestaurantePro.Mobile.Features.Operations.Comandas.Pages"
    xmlns:productos="clr-namespace:RestaurantePro.Mobile.Features.Operations.Productos.Pages"
    xmlns:preparaciones="clr-namespace:RestaurantePro.Mobile.Features.Inventory.Preparaciones.Pages"
    xmlns:reservaciones="clr-namespace:RestaurantePro.Mobile.Features.Inventory.Reservaciones.Pages"
    xmlns:facturas="clr-namespace:RestaurantePro.Mobile.Features.Commercial.Billing.Pages"
    xmlns:clientes="clr-namespace:RestaurantePro.Mobile.Features.Commercial.Customers.Pages"
    xmlns:tarjetas="clr-namespace:RestaurantePro.Mobile.Features.Commercial.Loyalty.Pages"
    xmlns:ingredientes="clr-namespace:RestaurantePro.Mobile.Features.Inventory.Ingredients.Pages"
    xmlns:categorias="clr-namespace:RestaurantePro.Mobile.Features.Categorias.Pages"
    xmlns:analytics="clr-namespace:RestaurantePro.Mobile.Features.Analytics.Pages"
    Title="RestaurantePro">

    <!-- Shell Resources -->
    <Shell.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.BackgroundColor" Value="#007ACC" />
                <Setter Property="Shell.ForegroundColor" Value="White" />
            </Style>
        </ResourceDictionary>
    </Shell.Resources>

    <!-- Login Page - Página inicial por defecto -->
    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="login" />

    <!-- Main App - Con navegación por tabs -->
    <TabBar Route="main">
        <!-- Dashboard Tab -->
        <ShellContent
            Title="Dashboard"
            Icon="home.png"
            ContentTemplate="{DataTemplate ui:DashboardPage}"
            Route="dashboard" />

        <!-- Mesas Tab -->
        <ShellContent
            Title="Mesas"
            Icon="table.png"
            ContentTemplate="{DataTemplate mesas:MesasPage}"
            Route="mesas" />

        <!-- Comandas Tab -->
        <ShellContent
            Title="Comandas"
            Icon="order.png"
            ContentTemplate="{DataTemplate comandas:ComandasPage}"
            Route="comandas" />

        <!-- Productos Tab -->
        <ShellContent
            Title="Productos"
            Icon="menu.png"
            ContentTemplate="{DataTemplate productos:ProductosPage}"
            Route="productos" />

        <!-- Preparaciones Tab -->
        <ShellContent
            Title="Cocina"
            Icon="kitchen.png"
            ContentTemplate="{DataTemplate preparaciones:PreparacionesPage}"
            Route="preparaciones" />

        <!-- Reservaciones Tab -->
        <ShellContent
            Title="Reservas"
            Icon="calendar.png"
            ContentTemplate="{DataTemplate reservaciones:ReservacionesPage}"
            Route="reservaciones" />

        <!-- Facturas Tab -->
        <ShellContent
            Title="Facturar"
            Icon="invoice.png"
            ContentTemplate="{DataTemplate facturas:FacturasPage}"
            Route="facturas" />

        <!-- Clientes Tab -->
        <ShellContent
            Title="Clientes"
            Icon="users.png"
            ContentTemplate="{DataTemplate clientes:ClientesPage}"
            Route="clientes" />

        <!-- Tarjetas Fidelización Tab -->
        <ShellContent
            Title="Fidelización"
            Icon="card.png"
            ContentTemplate="{DataTemplate tarjetas:TarjetasFidelizacionPage}"
            Route="tarjetasfidelizacion" />

        <!-- Ingredientes Tab -->
        <ShellContent
            Title="Ingredientes"
            Icon="ingredients.png"
            ContentTemplate="{DataTemplate ingredientes:IngredientesPage}"
            Route="ingredientes" />

        <!-- Categorías Tab -->
        <ShellContent
            Title="Categorías"
            Icon="category.png"
            ContentTemplate="{DataTemplate categorias:CategoriasPage}"
            Route="categorias" />

        <!-- Analytics Tab -->
        <ShellContent
            Title="Analytics"
            Icon="analytics.png"
            ContentTemplate="{DataTemplate analytics:AnalyticsPage}"
            Route="analytics" />
    </TabBar>

</Shell>
